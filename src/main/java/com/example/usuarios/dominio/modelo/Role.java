package com.example.usuarios.dominio.modelo;

/**
 * Enum que representa los roles de usuario en el sistema.
 * Principio SOLID: Single Responsibility - Solo define los roles disponibles.
 */
public enum Role {
    USER,
    ADMIN
}
